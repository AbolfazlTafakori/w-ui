---
description: "هر client یک پلن است با تعدادی اتصال هم‌زمان مجاز؛ هر فایل دستگاه کلید و آدرس خودش را دارد."
---

# مشتری‌ها

هر client یک پلن است با تعدادی **اتصال هم‌زمان** مجاز. هر فایل دستگاه (account) کلید و آدرس خودش را دارد و بیشتر از تعداد اتصال مجاز، فایل صادر نمی‌شود. عددِ کنار نام، تعداد جاهایی است که مشتری همین حالا از آن‌ها وصل است — از روی آدرس‌های عمومی زندهٔ اعتبارش شمرده می‌شود؛ یک فایل که روی دو دستگاه به‌نوبت استفاده شود یک اتصال است، هر دو هم‌زمان دو اتصال.

## کارت خلاصه

مشتری‌ها، آنلاین، تمام‌شده، در حال اتمام، غیرفعال، فعال — شش عدد هر کدام با یک نقطهٔ رنگی؛ روی گوشی دو تا در هر ردیف.

## نوار ابزار

**Add Clients** (یکی، یا چند تا از یک پیشوند و یک تعداد)، **more** (فعال / غیرفعال / حذف / بازنشانی / تمدید دسته‌ای برای انتخاب‌شده‌ها، خروجی)، و با ردیف انتخاب‌شده یک **Delete**. زیرش نوار فیلتر: جست‌وجو (نام، یادداشت، sub id، کلید، رمز)، **Filter** با نشانگر تعداد فیلترهای فعال، مرتب‌سازی، پاک کردن همه، و «نمایش از کل».

## جدول

| ستون | |
|------|--|
| Actions | QR، اطلاعات مشتری، بازنشانی ترافیک، ویرایش، حذف |
| Enabled | سوییچ؛ وقتی پلن منقضی یا تمام‌شده باشد غیرفعال است |
| Online | با آخرین باری که دیده شده |
| Client | نام، اتصال‌های فعال / مجاز هم‌زمان (بالاتر از سقف قرمز)، یادداشت |
| Group | کلیک کن تا فیلتر شود |
| Attached inbounds | پلن روی کدام اینترفیس‌هاست |
| Traffic | مصرف، نوار، سقف — قرمز بعد از ۱۰۰٪، نارنجی نزدیک آن |
| Speed | نرخ زنده |
| Remaining | حجم باقی‌مانده |
| Duration | زمان باقی‌مانده، یا ∞ |

صفحه‌بندی با تغییر اندازه بعد از ده ردیف. روی گوشی: کارت با نقطهٔ وضعیت، نوار ترافیک، و یک منو.

## QR

یک QR برای هر دستگاه، برای هر هاست — مشتری روی دو هاست، دو کد به ازای هر دستگاه دارد. قابل دانلود به PNG.

## اطلاعات مشتری

همه چیز یک پلن روی یک صفحه: وضعیت، سهمیه، انقضا، دستگاه‌ها، همهٔ لینک‌ها (فایل کانفیگ، سابسکریپشن، به ازای هاست)، شناسهٔ تلگرام، sub id، آخرین بار کِی و از کجا دیده شده.

## وضعیت‌ها

`active`، `disabled`، `exhausted`، `expired`. هر چیزی جز `active` peerهایش از کرنل حذف می‌شود؛ بالا بردن حجم یا تمدید تاریخ فوراً برمی‌گرداند.

## شروع از اولین استفاده

پلن می‌تواند به‌جای تاریخ ثابت، «۳۰ روز از اولین اتصال» فروخته شود: ساعت با اولین handshake شروع می‌شود.

در فرم مشتری کلید **On hold** را روشن کن و **طول پلن** را به روز بده. تا وقتی مشتری وصل نشده، ردیف برچسب **On hold** دارد و ستون انقضا تعداد روزها را نشان می‌دهد؛ با اولین اتصال شمارش شروع می‌شود و تاریخ انقضا ظاهر می‌شود.

## اتصال هم‌زمان

**اتصال هم‌زمان** (سقف دستگاه) اعمال می‌شود، نه فقط نمایش. هر دو ثانیه پنل نگاه می‌کند کدام اعتبارهای مشتری ترافیک زنده دارند و از کجا؛ وقتی بیشتر از سقف پلن وصل باشند، جدیدترین‌ها **دو دقیقه نگه داشته می‌شوند** — peer وایرگارد برداشته می‌شود و با پایان مهلت خودش برمی‌گردد، نشست OpenVPN قطع می‌شود. دستگاهی که از قبل وصل بوده می‌ماند. لاگ می‌گوید `connection limit reached; device held off` با نام مشتری، دستگاه و تا کِی.

شمارش همهٔ سرورها را می‌گیرد: دستگاهی روی نود هم یکی از اتصال‌های مشتری است و اگر باید نگه داشته شود، از طریق نود نگه داشته می‌شود — [نودها](/fa/panel/nodes) را ببین.

چه چیزی شمرده می‌شود: اعتباری که در ۷۵ ثانیهٔ اخیر بایت جابه‌جا کرده یک اتصال است، از هر آدرسی که باشد — گوشی‌ای که از وای‌فای به اینترنت همراه می‌رود یک بار آدرس عوض می‌کند و همان یک نفر است. یک فایل که هم‌زمان روی دو دستگاه استفاده شود، آدرسش مدام بین دو جا می‌پرد و آن دو اتصال حساب می‌شود. پس مشتری با ده فایل و پلن یک‌نفره هر کدام را به‌نوبت می‌تواند استفاده کند؛ دو تا با هم، دومی نگه داشته می‌شود.

## نام کاربری و رمز OpenVPN

OpenVPN به‌جای کلید با نام کاربری و رمز وارد می‌شود. وقتی بین سرورهای انتخاب‌شده برای مشتری یک تانل OpenVPN باشد، دو فیلد زیر انتخاب‌گر سرور ظاهر می‌شود: **نام کاربری OpenVPN** و **رمز OpenVPN**. چیزی را که مشتری باید استفاده کند تایپ کن، یا خالی بگذار تا پنل خودش بسازد. در فرم ویرایش، نام کاربری فعلی نشان داده می‌شود؛ رمز فقط وقتی باید تایپ شود که قرار است عوض شود.

همین اطلاعات روی هر تانل OpenVPN که مشتری روی آن است اعمال می‌شود. دستگاه دوم روی همان تانل، همین نام را با یک شماره می‌گیرد (`roya-2`)، چون تانل نشست‌ها را با نام می‌شناسد. نام کاربری روی هر تانل یکتاست، ۳ تا ۴۸ کاراکتر از حروف، ارقام و `- _ . @`؛ رمز ۶ تا ۶۴ کاراکتر بدون فاصله.
